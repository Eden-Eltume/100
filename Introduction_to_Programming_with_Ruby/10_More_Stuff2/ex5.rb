# Because we did not put prefix the block with the & symbol necassary to interpret it as block
# and not just a regular parameter.
# Right now the function is interpreting the parameter as just a regular parameter and not a block.
# and the block does not take a parameter either.
